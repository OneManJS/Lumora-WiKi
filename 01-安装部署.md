# 1. 安装部署

[返回使用指南](README.md)

## 部署前准备

- 已安装 Docker Engine 24+ 和 Docker Compose v2
- 服务器已放行 TCP 端口 `18096`
- 已准备应用数据、日志、数据库、Redis 和媒体目录
- PostgreSQL 密码与 `TOKEN_SECRET` 已替换为足够长的随机字符串

下面以 Linux 服务器为例。Windows 和 macOS 可以使用 Docker Desktop，但需要将卷左侧的宿主机路径改为本机实际路径。

## Docker Compose 部署（推荐）

创建 `docker-compose.yml`：

```yaml
name: lumora

services:
  app:
    container_name: lumora
    # 镜像仓库暂时沿用更名前的发布地址。
    image: onemanjs/lumora:latest
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://lumora:请替换数据库密码@postgres:5432/lumora
      TOKEN_SECRET: 请替换为至少32位的随机字符串
      CACHE_BACKEND: redis
      REDIS_URL: redis://redis:6379/0
      REDIS_REQUIRED: "true"
      MEDIA_ROOTS: /media
      UID: 0
      GID: 0
      TZ: Asia/Shanghai
    ports:
      - "18096:18096"
    volumes:
      - /data/lumora/instance:/app/instance
      - /data/lumora/logs:/app/logs
      - /path/to/media:/media:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://127.0.0.1:18096/readyz"]
      interval: 30s
      timeout: 5s
      start_period: 45s
      retries: 3

  postgres:
    container_name: lumora-postgres
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: lumora
      POSTGRES_USER: lumora
      POSTGRES_PASSWORD: 请替换数据库密码
      TZ: Asia/Shanghai
    volumes:
      - /data/lumora/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lumora -d lumora"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    container_name: lumora-redis
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1"]
    volumes:
      - /data/lumora/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

networks:
  default:
    name: lumora
```

启动前必须修改以下配置：

| 配置 | 说明 |
| --- | --- |
| 两处数据库密码 | `DATABASE_URL` 和 `POSTGRES_PASSWORD` 必须保持一致；密码包含 `@`、`:`、`/` 等字符时需要进行 URL 编码 |
| `TOKEN_SECRET` | 登录凭证签名密钥，至少 32 位；部署后不要随意更换，否则现有登录状态会失效 |
| `/path/to/media` | 改为宿主机真实媒体目录；容器内统一映射到 `/media` |
| `UID` / `GID` | 按部署环境设置容器访问文件所需的用户和用户组 ID |

> 示例将媒体目录以只读方式挂载。需要通过拾光 · Lumora 修改或删除媒体文件时，去掉卷配置末尾的 `:ro`，并确认目录权限设置正确。

在 `docker-compose.yml` 所在目录运行：

```bash
docker compose config
docker compose pull
docker compose up -d
docker compose ps
```

`app`、`postgres` 和 `redis` 均显示 `running` 或 `healthy` 后，访问 **`http://服务器地址:18096/`**。

## 启动检查

| 现象 | 处理方式 |
| --- | --- |
| 页面暂时打不开 | 首次启动等待约 30 秒，然后运行 `docker compose logs --tail=200 app` 查看日志 |
| `postgres` 不健康 | 检查两处数据库密码是否一致，并确认旧数据目录使用的账号配置没有变化 |
| 端口被占用 | 将端口映射左侧的 `18096` 改为其他空闲端口，例如 `19096:18096` |
| 媒体目录不可见 | 检查宿主机路径、容器内 `/media` 路径和文件读取权限 |
| NAS 新文件未自动入库 | 参阅[媒体库管理中的网络盘说明](03-媒体库管理.md#5-网络盘nas注意事项) |

也可以直接检查服务状态：

```bash
curl --fail http://127.0.0.1:18096/healthz
```

首次访问会进入[创建管理员账号](02-首次使用.md#2-创建管理员账号)向导。

## 数据备份

停止服务后，备份以下宿主机目录：

- `/data/lumora/instance`：应用配置和本地数据
- `/data/lumora/postgres`：PostgreSQL 数据
- `/data/lumora/redis`：Redis 持久化数据
- `/data/lumora/logs`：运行日志，排障需要时保留

媒体文件应按自己的存储方案单独备份。恢复时应使用与备份版本兼容的镜像，并保持数据库账号、密码和目录映射一致。

## 升级版本

1. 备份应用与数据库数据。
2. 在 Compose 目录运行 `docker compose pull`。
3. 运行 `docker compose up -d` 重建应用容器。
4. 使用 `docker compose ps` 和 `/healthz` 检查状态，再查看关键功能是否正常。

> 从 NextMediaHub 升级的现有部署应继续使用原来的 Compose 项目名、数据库名、账号和卷路径，只更新镜像并重建容器。直接套用本文的新安装路径会创建一套空数据目录，看起来像数据丢失。
