# 1. 安装部署

[← 返回目录](README.md)

##  Docker Compose（推荐）

项目自带的 `docker-compose.yml` 已包含 NextMediaHub、PostgreSQL 数据库和 Redis 缓存，一条命令启动：

```yml
name: nextmediahub

services:
  app:
    container_name: nextmediahub
    image: onemanjs/nextmediahub:latest

    restart: unless-stopped
    environment:
      # 建议修改pg库密码，和下面保持一致
      DATABASE_URL: postgresql+asyncpg://nextmediahub:NextMediaHub_Postgres_2026@postgres:5432/nextmediahub
      CACHE_BACKEND: redis
      REDIS_URL: redis://redis:6379/0
      REDIS_REQUIRED: "true"
      MEDIA_ROOTS: /media
      GUID: 0
      GPID: 0
      TZ: Asia/Shanghai
    ports:
      - "18096:18096"
    volumes:
        # 映射配置文件，改成自己的，只改前面
      - /data/NextMediaHub/instance:/app/instance
        # 映射日志文件，改成自己的，只改前面
      - /data/NextMediaHub/logs:/app/logs
        # 映射媒体文件，前后随便你改
      - /t/Media/测试:/Media
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://127.0.0.1:18096/readyz"]
      interval: 30s
      timeout: 5s
      start_period: 45s
      retries: 3

  postgres:
    container_name: nextmediahub-postgres
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: nextmediahub
      POSTGRES_USER: nextmediahub
      # 建议修改pg库密码，和上面保持一致
      POSTGRES_PASSWORD: NextMediaHub_Postgres_2026
      TZ: Asia/Shanghai
    volumes:
        # 映射数据文件，改成自己的，只改前面
      - /docker/NextMediaHub/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nextmediahub -d nextmediahub"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    container_name: nextmediahub-redis
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1"]
    volumes:
        # 映射数据文件，改成自己的，只改前面
      - /docker/NextMediaHub/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

networks:
  default:
    name: nextembyhub
```

```bash
docker compose up -d
docker compose ps        # 三个容器都应为 running/healthy
```

启动完成后浏览器打开 **`http://服务器IP:18096/`** 即可。

启动前建议检查 `docker-compose.yml` 中的三处配置：

| 配置位置 | 说明 |
| --- | --- |
| `POSTGRES_PASSWORD` | 数据库密码，换成足够长的随机字符串 |
| `TOKEN_SECRET` | 登录凭证签名密钥，至少 32 位随机字符 |
| 媒体目录挂载 | 默认把 `./media` 挂进容器；改成你实际的影片文件夹路径 |

数据都保存在 compose 目录下的 `./data/`（数据库、缓存、应用数据）与 `./logs/`（日志），备份这些目录即可。


## 启动后

浏览器打开服务地址，第一次会进入[创建管理员账号](02-首次使用.md#2-创建管理员账号)向导。

| 情况 | 处理 |
| --- | --- |
| 页面打不开 | 等 30 秒再刷新；仍不行看 `docker compose logs app` 或 `logs/` 目录日志 |
| 端口被占用 | 在 compose 文件或 `PORT` 环境变量里换一个端口 |
| 媒体在 NAS/网络盘上 | 见[媒体库管理 · 网络盘注意事项](03-媒体库管理.md#3-网络盘nas-注意事项) |
| 想用 Emby 客户端连接 | 见[首次使用 · 用 Emby 客户端观看](02-首次使用.md#5-用-emby-客户端观看) |

## 升级版本

1. 备份数据目录（Docker 部署备份 `./data/`，源码部署备份 `instance/`）。
2. 拉取新镜像 `docker compose pull` 后 `docker compose up -d`；源码部署 `git pull` 后重启 `python start.py`。
3. 数据库结构升级在启动时自动完成，不要在旧版本运行时叠加升级。
