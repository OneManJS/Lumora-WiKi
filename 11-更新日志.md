# 更新日志

## 1.0.10

### 元数据补全与任务整合

- 新增“运行设置 → 元数据”配置页，可按媒体库选择补全范围，并统一配置缺少标题、简介、图片、中文信息、季标题和正式剧照等补全规则；多项规则按任意一项命中处理，手动锁定的字段不会被覆盖。
- 元数据补全默认按首播日期检查最近 60 天内容，支持将回溯天数设为 `0` 以检查全部历史条目，也可决定是否包含无首播日期内容；“仅 NFO”媒体库默认不会联网，可按需允许 TMDB 参与补全。
- 新增无结果冷却与跨轮重试状态。提供方暂时无结果或请求失败时会按规则延后重试，减少重复请求；任务运行期间修改设置只影响下一轮任务。
- 精简刮削相关计划任务，统一为“全库扫描”“元数据补全”和“豆瓣缓存构建”。升级时会停用并隐藏旧的全量刷新、近期刷新和单集刷新任务，同时沿用原有启停状态、执行时间和回溯范围；历史任务记录不会删除。
- 豆瓣缓存改为增量维护，并可统一配置评分更新周期、季表周期以及标题简介、评分、演职员和角色名等辅助范围；所有媒体库共用串行通道，降低触发限流的概率。

### 媒体库扫描、图片与刷新

- 重构全库扫描为文件系统与数据库对账：成功遍历的范围会同步新增、变更和已删除内容；路径暂时不可访问时会报告异常并保留已有索引，避免网络盘离线造成误删。
- 新增内容只会对本次发现的媒体执行识别与刮削，降低大库增量扫描时的无效工作量。媒体库菜单新增全量“刷新元数据”，可覆盖电影、剧、季和集，默认保留已有图片并补缺，也可选择替换全部已有图片。
- 支持从 TMDB 获取电影和剧集 Logo，并可在图片管理中查看、上传、替换和删除；季与单集会继承所属剧集的 Logo。
- 剧集详情的季选择器新增季海报，修复季海报获取与展示异常；单集刷新现在可以更新延迟发布后的正式集名和剧照。
- 修复远程封面回退、缺失封面筛选、图片完整链接保存及图片提供方错配问题。手动放置的本地旁挂图会被正确识别，远程图片不可用时也能按候选来源继续回退。

### 媒体删除

- 新增按季删除，删除前会预览实际文件范围；独立季目录可随季删除，共用剧集目录时只处理该季内容，避免误删其他季。
- 批量删除改为后台任务，提交后媒体会立即从常规列表隐藏，并在后台处理源文件、索引、图片绑定与缓存；任务取消时可恢复可见性，失败状态也会保留以便排查。
- 优化删除后的查询缓存和图片资产同步，共享图片仍被其他媒体引用时会继续保留。

### 升级与稳定性

- 修复 1.0.9 数据库迁移在大媒体库、迁移中断及 PostgreSQL 索引创建场景下可能失败的问题。历史状态改为分批回填，迁移被中断后可安全重试；同时修复相关 SQLite 迁移兼容问题。
- 修复 SQLite 环境下重置密码可能出现的数据库死锁。
- 通知与 SMTP 凭据加密密钥在单实例部署中可自动生成并持久化到 `instance/notification_secret_key`，不再要求首次部署时手动填写。多实例且不共享 `instance` 目录时，仍必须为所有实例配置相同的 `NOTIFICATION_SECRET_KEY`。

> 从 1.0.9 升级时会自动执行数据库迁移，新增元数据任务状态、Logo、首播日期、计划任务迁移记录及媒体删除生命周期相关字段和数据表，升级前建议备份数据库与 `instance` 目录。首次启动会自动收敛旧刮削计划任务；请在升级后前往“运行设置 → 元数据”核对媒体库范围、补全规则、Logo 与豆瓣设置。若 1.0.9 的迁移曾中断，可直接使用 1.0.10 重新执行迁移。多实例部署请先统一通知加密密钥，再启动各实例。

<!-- changelog-maintainer: 1.0.10 boundary=3a0a4c5; range=ce1db00..3a0a4c5; next release=3a0a4c5..HEAD -->

## 1.0.9

### 账号与访问安全

- 新增安全中心，集中提供登录记录查询、手动 IP 封禁与解封，并可配置账号锁定、IP 自动封禁、滑块验证码、日志保留周期及可信代理规则。
- 登录保护支持账号与 IP 双维度限流：连续失败可触发滑块验证码、临时锁定或自动封禁；私网与回环地址默认不会被自动封禁，兼顾家庭网络和内网穿透场景。
- 完善登录会话安全，退出登录会在服务端撤销当前令牌；修改密码、管理员重置密码或找回密码后，原有登录会话将统一失效。
- 新增邮箱绑定和密码找回。管理员配置 SMTP 后，用户可在账号页验证邮箱，并通过重置链接或邮箱验证码找回密码；SMTP 支持 SSL、STARTTLS、连通性检查和测试邮件。
- 登录日志、限流与封禁可按可信代理链识别真实客户端 IP，适配直连、frp、VPS 反向代理和 Cloudflare Tunnel/CDN 等部署方式。

### 管理员通知与消息中心

- 新增独立的管理员消息中心和控制台通知铃铛，服务状态、任务失败、求片、安全及系统事件可实时进入站内消息，并显示未读状态。
- 新增 Telegram、企业微信、飞书、Slack、Discord、钉钉、群晖 Chat、VoceChat、QQ 机器人和自定义 Webhook 共 10 类通知渠道；每个渠道可独立启停并选择订阅事件。
- 通知渠道支持保存前试发、投递失败自动重试和投递状态记录；敏感凭据加密保存。
- 管理员通知与面向普通用户的 WxPusher 通知相互独立，原有 WxPusher 和 Webhook 配置仍保留在通知页面中。

### 扫描、刮削与任务调度

- 重构实时监听的增量入库流程，改为有界分批处理并缩小目录对账范围；大量文件事件会优先压缩为局部目录任务，避免轻易退化为全库扫描，降低内存和磁盘压力。
- 优化刮削任务合并与公平调度，同一批媒体变化不再反复创建重复任务，剧集与恢复任务也不会因持续新增任务而长期等待。
- 新增明确的元数据处理状态，区分“已匹配”“确认无结果”和“等待重试”；第三方服务超时、限流或异常时不再误判为无匹配，减少无效重复刮削。
- 计划任务检测到重复的刮削职责时会显示提示，并支持一键合并为统一恢复任务；任务中心补充任务范围、来源、重跑次数和后续任务数，排查积压更直观。
- 优化 SQLite 写事务与实时扫描之间的竞争，减少任务续租、计划任务和监听入库并发时出现 `database is locked`；PostgreSQL 继续使用原生并发事务能力。
- STRM 指纹重算改由后台任务执行，避免在管理操作中长时间阻塞；修复文件系统执行器繁忙及监听事件并发收窄时的稳定性问题。

### 元数据、图片与媒体体验

- 统一媒体探测、片头片尾识别、元数据请求和图片下载的运行时 QPS 设置，调整后立即生效；豆瓣请求保留独立的安全间隔，避免影响其他元数据来源。
- 优化豆瓣中文元数据补全，兼容更多演职员字段，并支持将新的合并规则重新应用到历史媒体；TMDB 单集详情会在季数据不完整时继续补充中文标题、简介和剧照。
- 新增“始终返回原图”模式，可跳过缩略图与尺寸缓存；图片缓存清理改为后台任务。删除媒体时会同步清理不再使用的图片绑定、旁挂图和派生缓存，同时保留仍被其他媒体共享的资产。
- 优化搜索结果分页和 Emby 全类型媒体目录查询，大型媒体库的浏览与排序更流畅；修复合并剧集的未看集数统计不准确问题。

### 设置与界面优化

- 运行设置重构为通用、QPS、媒体库、播放、安全和授权等分区，采用更清晰的卡片与分节布局。
- 常用设置改为修改即生效：开关和下拉选择会立即保存，文本及数字输入在失焦或按回车时保存，失败后会提示并恢复服务端值。
- 修复明暗主题下未选中的开关和复选框辨识度不足，以及任务中心工具栏对齐和控件高度不一致的问题。
- 优化管理日志读取与慢请求归因，减少日志查看对服务的影响，并让接口与数据库耗时更易定位。

> 从 1.0.8 升级时会自动执行数据库迁移，新增安全、邮件找回、管理员通知及元数据状态相关数据表和索引。升级前建议备份数据库。密码找回需要管理员先在“运行设置 → 安全”中配置 SMTP 和对外访问地址，并由用户绑定邮箱；反向代理、frp 或 CDN 部署请同时正确配置可信代理网段或真实 IP 请求头，以免登录日志和自动封禁识别到代理地址。

<!-- changelog-maintainer: 1.0.9 boundary=ce1db00; range=be84646..ce1db00; next release=ce1db00..3a0a4c5 -->

## 1.0.8

### 网盘转发快速配置

- 新增一站式快速配置向导，可在同一流程中选择或新增网盘账号、浏览网盘目录、填写本地媒体路径并生成匹配规则，降低首次配置 115、123 网盘转发的门槛。
- 路径映射支持根据本地与网盘的完整样例路径智能推断，并可先预览规则匹配结果；账号既可复用已有配置，也可在向导中直接创建。
- 完善平台小号分配与全局兜底映射，未单独指定账号的用户也可按管理员配置完成解析；目录选择器现在可以点选文件并回填完整路径。
- 修复规则对象选择、双完整路径识别和 115 目录浏览等问题，并加强并发解析请求合并，减少同一媒体被重复解析。

### 媒体管理与搜索

- 管理员可直接在观影端的电影和剧集海报上打开管理菜单，完成元数据编辑、图片管理、重新识别、刷新元数据和删除操作，无需来回切换管理控制台。
- 电影、剧集与单集统一使用源文件删除流程；删除确认会列出将被删除的实际媒体路径，未找到磁盘文件时会明确提示仅移除索引，降低误删风险。
- 搜索框支持输入纯数字的 TMDB、豆瓣、TVDB 等外部数据源 ID，并在 SQLite 与 PostgreSQL 下精确匹配媒体的 Provider ID。
- 优化媒体墙多选与手机确认框：进入选择状态后点击海报即可连续多选，小屏确认操作更易使用。

### 图片资产与存储

- 新增按文件内容哈希管理的共享图片资产库，相同海报和背景图只需保留一份中心资产，并记录媒体绑定及远程来源，减少多版本媒体重复占用空间。
- “运行设置 → 偏好设置”新增图片存储方式，可选择“仅共享资产”“硬链接旁挂”或“独立旁挂副本”；同时提供“扫描存量图片”任务，用于归档并去重已有旁挂图。
- 图片缓存改用分桶目录，并在后台分批迁移旧缓存和远程图片；清理任务不再全量遍历所有文件，并会记录清理数量与节省空间等结果。
- 区分用户本地图片与远程下载图片的归档策略，完善共享模式下的旁挂清理、无引用资产回收和异常恢复，修复升级后缓存根目录遗留文件未清理的问题。
- 修复封面工作室解析分类图片提供方失败的问题，封面生成可继续正确使用对应图片来源。

### 媒体库、任务与界面

- 媒体库页面改为更紧凑的 Emby 风格卡片，编辑和删除操作收进悬浮菜单；删除确认展示媒体库路径，删除期间的状态、进度与停用标识也更清晰。
- 任务中心的活动任务按任务类型归并展示，避免同类后台任务重复占据概览位置。
- 统一多版本媒体标签中的音频声道数字写法，并过滤值为 `None` 的无效 HDR 标签。
- 修复多处移动端导航、弹窗、表单和布局问题，改善窄屏下的管理与观影体验。

### 稳定性修复

- 修复“接着看下一集”开关重启后恢复默认值的问题，保存后的站点设置现在会在服务启动时正确加载。
- 媒体库监听根目录异常时不再误触发全库扫描，避免挂载暂时不可用造成高开销后台任务。
- 修复图片资产删除计数、共享图片去重、缓存迁移与清理过程中的边界问题，提高升级和大批量图片处理的可靠性。

> 从 1.0.7 升级时会自动执行数据库迁移，并在后台整理旧图片缓存目录；迁移失败不会阻止服务启动，下次启动会继续尝试。媒体目录中的存量旁挂图不会自动全量去重，如有需要可在“运行设置 → 偏好设置”中选择图片存储方式后手动运行“扫描存量图片”。Docker 部署通常建议使用“仅共享资产”并持久化 instance 目录；“硬链接旁挂”要求中心图片资产与媒体目录位于同一文件系统。

<!-- changelog-maintainer: 1.0.8 boundary=be84646; range=25904b5..be84646; next release=be84646..HEAD -->

## 1.0.7

### 网盘转发（测试功能）

> **注意：网盘转发目前仍是测试功能，尚未完成全面验证，不建议在生产环境中将其作为唯一播放链路。请先使用少量测试媒体验证账号、规则和回退策略，并保留原有播放方式。**

- 新增 115 与 123 网盘转发测试能力，可将符合规则的媒体解析为网盘直链并通过 302 跳转播放；支持路径替换、网盘文件标识和 Tdr-302 标准链接等匹配方式。
- 新增独立的“网盘转发”管理页，集中提供运行概览、账号、匹配规则、小号与用户、转发测试、解析日志和全局设置；目录可通过选择器完成配置，无需手填目录 ID。
- 支持管理员配置主号和平台小号，也支持用户在观影端绑定自己的 115 或 123 账号。可按用户分配小号，并配置解析失败后的阻止、回退等处理策略。
- 增加账号扫码或凭据登录、令牌刷新与临期提醒、账号健康检查、规则试匹配、真实媒体全链路测试及分步诊断，敏感凭据在管理接口中统一脱敏。
- 增加解析缓存、失败退避、账号熔断、并发请求合并、秒传临时文件登记与自动清理；网盘解析结果、耗时和失败原因可在日志中查询。
- 网盘转发时间统一按北京时间显示，并修复 123 秒传信息回填、115 缓存目录选择、目录空态、账号权限隔离和统计等问题。

### 手机与 PWA 体验

- 完善手机端观影页和管理端布局，表格在窄屏下自动转换为摘要列表，导航、抽屉、弹窗、下拉选择和页签均针对触屏操作优化。
- 新增 PWA 安装支持及 Android、iOS 安装引导，补充应用图标、安全区域、动态主题色和移动端页面转场。
- 优化移动播放器控制栏、进度条触控区域、横竖屏提示和全屏兼容；详情页、剧集列表及常用管理页面在小屏下更易浏览和操作。

### 播放、剧集与界面

- 修复预备播放会话重复显示、浏览媒体被误计为播放，以及同一设备产生多条活跃会话的问题；外部播放器真正开始拉流后仍可正确显示会话并维持心跳。
- “运行设置 → 偏好设置”新增“接着看下一集”站点开关，保存后立即生效；并修复指定剧集时待看队列的兼容问题。
- 单集标题可直接进入剧集详情；剧集列表接口上限提升至 10000，改善大季集内容在第三方客户端中的加载完整性。
- 管理员未启用 WxPusher 时，观影端不再显示无效的通知绑定卡片。
- 优化版本升级后的前端缓存处理，减少发布后仍加载旧页面资源的情况。

### 媒体库与第三方兼容

- 加强实时监听的事件合并、关闭落库、挂载诊断和按目录降级，修复监听遗漏、重复触发以及 worker 故障误触发全库扫描的问题。
- 兼容 SA 等工具按媒体位置发起针对性扫描，避免只能刷新整个媒体库；修复 Emby 数据迁移请求头含中文时的编码错误。
- 优化媒体探测失败后的退避和重试，避免同一异常文件被连续高频探测，同时允许临时故障恢复后再次处理。
- 修复 TMDB 季、集请求校验及旧版负缓存兼容问题，降低剧集元数据因历史缓存或请求参数差异而漏刮的概率。

### 运维与诊断

- Emby 协议抓包补充请求与响应信息，便于排查实时会话和客户端兼容问题。
- 活动任务统计改为按任务类型归并，减少同类后台任务在概览中重复计数。
- 系统日志时间统一按北京时间显示，并减少正常 WebSocket 连接与断开产生的无效日志。
- 登录授权有效期调整为长期有效，减少正常使用中的重复登录。

> 从 1.0.6 升级时会自动执行网盘转发相关数据库迁移。该功能默认仍应按测试能力看待；升级前建议备份数据库，升级后先确认常规媒体扫描与播放正常，再单独进行网盘转发测试。

<!-- changelog-maintainer: 1.0.7 boundary=25904b5; range=87938ee..25904b5; next release=25904b5..be84646 -->

## 1.0.6

### 媒体库扫描与稳定性

- 重构大型媒体库扫描流程，缩短数据库事务并分批完成索引清理；删除媒体库或移除路径时改由后台任务渐进处理，显著降低 SQLite 锁库、扫描卡住和任务误失败的概率。
- 优化 Windows、Linux、Docker 与网络挂载环境下的目录遍历和文件状态读取。未变化的文件会尽量跳过重复处理，批量扫描及异常恢复更快、更稳。
- 修复目录监听异常时意外触发全库扫描、活动扫描任务删除引发锁冲突，以及扫描收尾阶段集中删除旧记录造成数据库繁忙的问题。
- `.strm` 的实际播放地址会在扫描时写入数据库，升级后自动回填旧数据。浏览和播放不再反复读取 `.strm` 文件，远程目录或大库中的响应速度更稳定。

### 播放进度与剧集体验

- 完善播放完成度、续播位置和重复观看次数的计算，同一次播放结束消息重复上报时不会重复计数；达到完成阈值后会正确清除续播位置。
- 修复剧集单集未出现在“继续观看”、按已看或未看筛选剧集不准确，以及重新识别剧集后单集元数据未同步更新的问题。
- 正确识别 `Specials` 目录为第 0 季，避免特别篇季号缺失或被错误归入普通季度。
- 改进多版本媒体的来源选择与播放地址兼容性，降低 Emby 协议客户端切换版本后无法播放的情况。

### Emby 与第三方客户端兼容

- 加强 Emby 接口对数字媒体 ID、剧集递归查询、媒体来源字段和工作室信息的兼容，恢复依赖数字 ID 的客户端及工具正常访问。
- 针对 Yamby 完善剧集排序、相似内容和媒体来源响应，修复部分页面为空、顺序异常或反序列化失败的问题。
- 优化 Emby 媒体列表、剧集树、相似内容及管理端媒体信息查询，并增加配套数据库索引；大型媒体库在 SQLite 与 PostgreSQL 下均可获得更稳定的读取性能。

### 元数据、图片与封面

- 封面工作室支持上传自定义字体，可在生成海报和媒体库封面时使用自己的字体文件。
- 刮削与图片下载统一遵循各媒体库设置的 QPS 限制，多个后台任务同时运行时也能更平稳地控制请求频率。
- 修复超长旁挂图片路径导致扫描失败的问题；无法安全写回原目录时，图片会转存至应用缓存继续使用。
- 优化相似内容候选查询和 PostgreSQL 媒体读取路径，减少重复解析与不必要的数据库开销。

### 账户、界面与升级

- 统一管理控制台常用控件的尺寸与圆角，修复统计卡片对齐、任务通道留白及求片页选项缺少可见文字等界面问题。
- 授权码会持久化保存，并在服务启动时自动完成在线校验，减少重启后的重复配置。
- 修复发布包遗漏数据库迁移文件及部分跨平台元数据、目录打包校验问题，提升 Docker 和安装包升级可靠性。

> 从 1.0.5 升级时会自动执行数据库迁移，并在后台回填旧 `.strm` 数据。大型媒体库首次启动后的整理时间可能比平时稍长，请等待后台任务完成，期间不要重复重启服务。

<!-- changelog-maintainer: 1.0.6 boundary=87938ee; range=c7982cd..87938ee; next release=87938ee..25904b5 -->

## 1.0.5

### 微信通知

- 新增 WxPusher 微信通知能力。管理员可在通知中心配置并测试渠道，用户可通过二维码或 UID 绑定微信，并分别控制追剧更新、求片到库和管理员通知。
- 收藏的剧集发现新单集时会发送追剧更新；同一集新增不同画质或压制版本不会重复提醒。求片完成入库后也会向提交用户发送通知。
- 管理员可向全部或指定的已绑定用户群发通知，发送前可预览接收人数；群发任务支持后台分批投递、进度与结果记录，以及停止尚未发送的批次。
- 完善二维码轮询、回调校验、UID 唯一绑定、重复事件去重和失败重试，降低重复通知、错误绑定和临时网络故障造成的漏发风险。

### 大型媒体库性能

- 为媒体库目录、媒体类型、流派、工作室、年份等常用目录接口接入版本化查询缓存；媒体或权限变化后自动失效，减少重复数据库查询。
- 优化 Emby 用户目录、全局最新媒体、NextUp、相似媒体、剧集单集树及媒体项目统计查询，并补充数据库索引；针对约 200 万条媒体数据的场景完成专项压测和查询收敛。
- 优化管理概览、人物作品、播放历史和多维使用统计，在高播放量或冷缓存场景下减少全表扫描、临时排序和大结果集加载。
- 缩短后台图片预缓存占用数据库连接的时间，降低大量图片任务与前台查询同时运行时的数据库争用。

### 媒体库监听与扫描

- 大型媒体库的实时监听改为按根目录隔离运行，单个目录异常不会拖垮其他媒体库；不同目录可分别使用原生监听或兼容轮询。
- 原生监听连续失败时会自动降级为低频轮询，并定期尝试恢复；新增周期性一致性核对，用于补偿 Docker、网络挂载或系统事件队列偶发遗漏的文件变化。
- 优化跨平台监听、事件合并和目录遍历，减少批量新增文件时的重复扫描、进程重启与内存占用，并增强监听状态和故障原因的可观测性。
- 修改或移除媒体库路径时改为延迟清理索引，避免大批量删除在请求事务中执行而引发超时或异常；清理过程由后台任务安全完成。

### 搜索与元数据

- 改进包含数字、标点和短符号的标题搜索，修复短剧名误匹配以及数字标题无法稳定命中的问题；Provider ID 不再混入普通标题搜索结果。
- 元数据图片下载增加受控重试，改善 TMDB、NFO 和本地化图片遇到连接中断、超时等临时网络故障时的成功率，同时合并重复失败日志，便于排障。
- 修复管理端编辑元数据服务 QPS 时的类型错误，保存限速配置不再因输入控件值类型不一致而失败。

### 权限、兼容性与界面修复

- 统一媒体路径访问权限检查并缓存用户的媒体库授权，减少列表、播放、会话和实时接口反复读取权限数据的开销；权限变更后会及时失效缓存。
- 修复 Emby 手动刷新错误复用已完成任务的问题，每次手动刷新都能正确创建或唤醒新的后台任务。
- 修复旧版 SQLAlchemy 环境下 NextUp 查询兼容问题，并改进部分客户端请求的查询兼容性。
- 完善管理端通知中心、账户绑定页和导航布局，修复统计卡片文字溢出等界面问题。

<!-- changelog-maintainer: 1.0.5 boundary=44c4a90; range=9798048..44c4a90; next release=44c4a90..HEAD -->

## 1.0.4

### 品牌升级

- 项目正式由 NextMediaHub 更名为「拾光 · Lumora」，网页标题、管理界面、服务信息和文档已统一使用新名称。
- 应用版本、安装包名称及新安装使用的默认 Docker 镜像名同步更新为 Lumora。
- 从 NextMediaHub 升级时，请保留原 Compose 项目名、数据库名、账号和卷路径，只替换镜像并重建容器。不要直接套用新安装示例中的 `/data/lumora` 路径，否则会挂载一套空目录，看起来像原有数据丢失。

### 虚拟库

- 虚拟库新增豆瓣榜单来源。可直接选择实时热门电影、热门剧集、热门综艺、电影 Top 250、一周口碑电影榜、热门动漫、正在热映、最新电影及不同地区热门剧集等预置榜单。
- 支持填写豆瓣榜单 ID 或链接，并可设置每次抓取的条目数量；榜单内容会先匹配 TMDB，再与本地媒体对应。
- 新增“保留已从榜单下架的条目”选项。开启后，榜单更新时暂时缺席的影片会保留并排在末尾；关闭后则不再显示。
- 修复虚拟库参与封面生成时条目选择不完整、封面统计不准确的问题，并修复 PostgreSQL 下虚拟库封面排序查询报错。

### 计划任务

- 计划任务新增可视化时间配置，可按间隔、每天、每周、每月或自定义 Cron 规则执行。
- 每周任务可直接选择多个星期，每月任务可指定日期；高级用户也可填写标准五段 Cron 表达式。
- 所有定时规则统一按北京时间（UTC+8）计算，并自动修正旧任务的下次执行时间。
- 兼容升级前创建、尚未包含 Cron 字段的计划任务，避免配置页面因旧数据无法正常显示。

### 媒体库监听

- 同一目录内短时间发生的多个文件变化现在会合并处理。批量复制、整理媒体或生成旁挂文件时，可减少重复扫描和任务堆积。
- 目录监听会忽略海报、背景图及下载临时文件产生的无关变化，减少图片刮削或缓存写入触发的额外扫描。
- 改进目录新增、移动和删除后的增量核对，只检查受影响的目录范围，并及时清理已经不存在的媒体记录。

### 第三方工具兼容

- 补充 MoviePilot 读取媒体库目录、查询媒体数量、获取远程图片及按媒体库刷新所需的 Emby 接口。
- 增强 Symedia 等整理工具的兼容性：支持递归刷新、按 TMDB 等外部 ID 查询，以及省略用户路径的“最近添加”查询。
- 修复第三方工具使用“媒体库 ID”或“媒体库路径 ID”发起刷新时无法正确定位媒体库的问题，提升 MoviePilot、Symedia 等工具触发媒体库刷新的成功率。
- 自动兼容第三方工具请求地址中意外出现的重复斜杠，减少连接或刷新失败。

### 观影体验与问题修复

- 修复切换剧集时上一集图片短暂闪现的问题，使海报和背景图切换更平滑。
- 改进虚拟库中已离开榜单条目的显示顺序，避免与当前榜单内容混排。

### Docker 镜像发布

- Docker Hub 与 GHCR 会同步发布新品牌 `lumora` 和旧品牌 `nextmediahub` 镜像名，旧部署在品牌迁移期间仍可继续获取更新。

<!-- changelog-maintainer: 1.0.4 boundary=9798048; range=f3c3259..9798048 -->

## 1.0.3

- 新增结构化元数据缓存库，支持豆瓣缓存导入及在线优先模式。
- 媒体图片管理增强：支持裁剪、TMDB 图库搜索和单集剧照。
- 优化元数据服务配置，支持分级 QPS 限速与运行时热更新。
- 改进任务中心、会话中心及管理端界面。
- 增强 Linux Docker 实时目录监控和异常恢复能力。
- 修复 TMDB 刮削断链、临时故障漏刮、任务堆积等稳定性问题。
- 优化首页加载与目录浏览体验。
