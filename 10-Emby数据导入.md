# 10. Emby 数据导入

[返回使用指南](README.md)

以前使用 Emby，现在想迁移到拾光 · Lumora？原服务器上的用户账号、观影记录、继续观看进度和收藏都可以导入。管理入口：**管理控制台 → 系统 → Emby 数据导入**。

> 开始前三件事：
> 1. **先建好媒体库并完成扫描刮削**。导入是按 TMDB ID 把 Emby 上的观看数据对应到本地影片的，本地没收录的片源匹配不上（会计入「未匹配」）；
> 2. **先检查原 Emby 账号的密码长度**。Emby 允许空密码，也可能不限制密码长度；拾光 · Lumora 的账号密码要求为 **8～512 个字符**。迁移前请为原 Emby 管理员和所有待迁移用户设置符合此范围的非空密码；
> 3. **选空闲时段执行**。导入会持续请求原 Emby 服务器并写入数据库，高峰期会影响正在看片的用户。
>
> 整个过程只搬「人」和「观看数据」，不会复制、移动任何媒体文件。

## 1. 连接原 Emby 并同步用户

1. 填写原 Emby 的**服务器地址**（如 `http://192.168.1.10:8096`）、**管理员账号**和**管理员密码**。密码只用于本次换取访问令牌，不会保存；
2. 点**测试连接**，提示「连接成功，已验证管理员 ×××」后继续；
3. 按需设置下面两项：
   - **允许用户沿用原密码首次登录**（默认开启）：导入的用户第一次登录时用原来的 Emby 密码，验证成功后自动转成本站密码，详见下一节；
   - **设置导入用户的权限组**（默认开启）：勾选一个或多个权限组，导入的用户会加入这些组（覆盖他们现有的权限组）；不开启则保持用户当前的媒体库权限；
4. 点**保存并同步用户**，完成后会提示：新建 × 个，关联 × 个，跳过 × 个。

| 结果 | 含义 |
| --- | --- |
| 新建 | 本地没有同名用户，创建新账号（初始处于「待迁移密码」状态） |
| 关联 | 本地已有同名（不区分大小写）用户，直接关联，不改动其密码 |
| 跳过 | 原 Emby 的**管理员账号不导入**（本站管理员由自己管理）；已导入过的用户重复执行也会跳过 |

新建用户会沿用原 Emby 的账号状态和权限策略：是否停用、是否允许转码、是否允许下载。

## 2. 用户沿用原密码首次登录

Emby 不提供密码哈希导出，所以导入的账号本地一开始「不知道」密码。开关打开时：

1. 导入用户用网页或 Emby 客户端登录，输入**原来的 Emby 密码**；
2. 系统拿这个密码去原 Emby 验证，通过后自动转换为本站密码；
3. 之后就完全不依赖原 Emby 了。

> 转换完成前需要**保持原 Emby 正常运行**。等所有用户都用原密码登录过一次，下次同步用户时就可以关掉这个开关。

> **密码兼容性注意事项**：Emby 可以使用空密码或超长密码，但拾光 · Lumora 新建、重置和修改密码时只接受 **8～512 个字符**，首次登录请求也最多接收 512 个字符。原 Emby 管理员使用空密码时无法保存导入连接；迁移用户使用空密码或超过 512 个字符的密码时，无法正常完成首次登录和密码转换。建议在迁移前先到原 Emby 修改这些密码。若用户已经导入，可在 **管理控制台 → 系统 → 用户与权限** 中为该用户重置一个符合规则的新密码；重置后直接使用新密码登录，不再需要原 Emby 密码。

## 3. 同步收藏与观影记录

确认页面显示「已关联 N 个 Emby 用户」后，点**同步收藏与观影记录**。系统逐个用户、分三轮拉取：**观看记录 → 继续观看 → 收藏记录**，页面上有进度条（当前用户、当前阶段）和累计统计。

- **观看记录**：看过的电影/剧集写入一条「已看完」的播放历史，最后观看时间、播放次数都从 Emby 带过来；
- **继续观看**：没看完的影片把播放进度带过来，用户打开即可从原位置续看；
- **收藏记录**：以原 Emby 的收藏为准覆盖本地收藏，电影、剧集、单集都支持。

统计里的**未匹配**数量偏多，通常是本地库还没收录或刮削失败：把片子补进库、刮削成功后再重新同步一次即可。

> 中途可以随时点**停止**，已写入的数据会保留；重新开始也不会重复导入——观影历史自动去重，收藏以最新一次同步为准。放心多次执行。

## 4. 导入完成后

- 提醒家人用**原 Emby 用户名 + 原密码**登录，网页或 Emby 客户端都可以（见[用 Emby 客户端观看](02-首次使用.md#5-用-emby-客户端观看)）；
- 检查每个用户能看哪些库：**管理控制台 → 系统 → 用户与权限**（见[用户与权限](06-用户与权限.md)）；
- 到**管理控制台 → 运行 → 播放活动**确认导入的观影记录正常。

## 常见问题

- **提示「请输入 Emby 管理员账号」**：必须用原 Emby 的管理员账号，普通用户不行；
- **提示「Emby 授权已失效，请重新保存连接」**：原 Emby 重启过或令牌过期了。重新填一次管理员密码、点**保存并同步用户**即可，已导入的数据不受影响；
- **提示「请至少选择一个权限组」**：开着「设置导入用户的权限组」就至少勾选一个组，或者把开关关掉；
- **用户在 Emby 可以无密码登录，迁移后却无法登录**：本站不支持空密码。请到**管理控制台 → 系统 → 用户与权限**为该用户重置一个 8～512 个字符的新密码；
- **原密码超过 512 个字符，首次登录失败**：首次登录请求最多接收 512 个字符。请先在原 Emby 将密码改为 8～512 个字符后重试，或由本站管理员直接重置密码；
- **未匹配很多**：本地媒体库还没扫描/刮削完成，先到[媒体库管理](03-媒体库管理.md)处理，再重新同步；
- **本地自己点的收藏没了**：收藏同步以原 Emby 为准覆盖本地。想保留本地收藏，先在原 Emby 上也收藏同一部影片再同步。

## 下一步

- 用户开好了，看看[观影使用](05-观影使用.md)里有哪些功能可以告诉家人
- 调整导入用户的库权限 → [用户与权限](06-用户与权限.md)
